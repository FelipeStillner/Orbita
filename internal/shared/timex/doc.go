// Package timex provides shared time-related utilities.
//
// Go’s standard library already uses the name "time", so this package is named timex
// to avoid collisions and to mirror common patterns (e.g. httpx, io extensions).
//
// Add more time helpers here (schedules, parsing, etc.) as needed.
package timex
