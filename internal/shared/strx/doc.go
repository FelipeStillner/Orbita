// Package strx provides shared string utilities.
//
// Go’s standard library already uses the name "strings", so this package is named strx
// to avoid confusion and to mirror common patterns (e.g. timex, httpx).
//
// Add more string helpers here (normalization, masking, etc.) as needed.
package strx
